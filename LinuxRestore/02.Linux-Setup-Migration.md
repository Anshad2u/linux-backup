---

## Phase 8: Guacamole Credentials Configuration

### Password Setup
Updated `/home/anshad/data/guacamole/user-mapping.xml` with secure password.

**Command:** `echo -n "Nopassword.123" | sha256sum | cut -d' ' -f1`
**Purpose:** Generate SHA-256 hash for secure password storage
**Output:** 50723a1985ff5c36415abf16f32176abe5ef0eb60f0e334daf60dd17daecf7a7

### user-mapping.xml Configuration
File location: `/home/anshad/data/guacamole/user-mapping.xml`

```xml
<user-mapping>
    <authorize username="guacadmin" password="50723a1985ff5c36415abf16f32176abe5ef0eb60f0e334daf60dd17daecf7a7" encoding="sha256">
        <connection name="Linux Desktop">
            <protocol>rdp</protocol>
            <param name="hostname">localhost</param>
            <param name="port">3389</param>
        </connection>
        <connection name="XRDP Session">
            <protocol>rdp</protocol>
            <param name="hostname">localhost</param>
            <param name="port">3389</param>
        </connection>
    </authorize>
</user-mapping>
```

**Purpose:** Configure Guacamole with secure password and RDP connections
**Backup:** Keep user-mapping.xml in ~/data/guacamole/ for persistence

### Guacamole Deployment with Config
**Command:**
```bash
docker run -d --name guacamole --restart unless-stopped \
  -p 8090:8080 \
  -v /home/anshad/data/guacamole/user-mapping.xml:/etc/guacamole/user-mapping.xml:ro \
  guacamole/guacamole:1.6.0
```

**Purpose:** Deploy Guacamole with persistent authentication config
**Volumes:**
- /home/anshad/data/guacamole/user-mapping.xml → /etc/guacamole/user-mapping.xml (read-only)

---

## Phase 9: Security & Best Practices

### 1. File Permissions
**Command:** `chmod 600 /home/anshad/data/guacamole/user-mapping.xml`
**Purpose:** Restrict access to authentication file
**Backup:** Preserve permissions with backup: `tar -czpvf ...`

### 2. Auto-Update Script
Create `/home/anshad/scripts/update.sh`:

```bash
#!/bin/bash
# Auto-update script for Docker containers

# Update Guacamole
docker pull guacamole/guacamole:1.6.0
docker stop guacamole
docker rm guacamole
docker run -d --name guacamole --restart unless-stopped \
  -p 8090:8080 \
  -v /home/anshad/data/guacamole/user-mapping.xml:/etc/guacamole/user-mapping.xml:ro \
  guacamole/guacamole:1.6.0

# Prune old images
docker image prune -f

echo "Update complete: $(date)"
```

**Command:** `chmod +x /home/anshad/scripts/update.sh`
**Purpose:** Keep containers updated
**Schedule:** Add to cron: `0 3 * * 0 /home/anshad/scripts/update.sh`

### 3. Health Check Script
Create `/home/anshad/scripts/health-check.sh`:

```bash
#!/bin/bash
echo "=== Health Check $(date) ==="

# Check Coolify
echo "Coolify: $(curl -s -o /dev/null -w "%{http_code}" http://localhost:8000)"

# Check Guacamole
echo "Guacamole: $(curl -s -o /dev/null -w "%{http_code}" http://localhost:8090/guacamole/)"

# Check Docker containers
sudo docker ps --format "table {{.Names}}\t{{.Status}}"

# Check Cloudflared
pgrep cloudflared > /dev/null && echo "Cloudflared: Running" || echo "Cloudflared: STOPPED"
```

**Command:** `chmod +x /home/anshad/scripts/health-check.sh`
**Purpose:** Quick status verification
**Usage:** Run manually or via cron

### 4. Backup Script Enhancement
Update `/home/anshad/scripts/backup.sh`:

```bash
#!/bin/bash
BACKUP_DIR="${1:-/backup/$(date +%Y%m%d)}"
mkdir -p "$BACKUP_DIR"

echo "Backing up to $BACKUP_DIR..."

# Backup critical data
tar -czpf "$BACKUP_DIR/guacamole.tar.gz" -C /home/anshad/data/guacamole .
tar -czpf "$BACKUP_DIR/coolify-config.tar.gz" /data/coolify/source/.env 2>/dev/null || true
tar -czpf "$BACKUP_DIR/projects.tar.gz" -C /home/anshad/projects . 2>/dev/null || true

# Backup scripts
cp -r /home/anshad/scripts "$BACKUP_DIR/"

# Export Docker configs
docker inspect guacamole > "$BACKUP_DIR/guacamole-docker.json" 2>/dev/null || true

echo "Backup complete: $BACKUP_DIR"
```

**Purpose:** Comprehensive backup of all services and data
**Schedule:** Daily via cron: `0 2 * * * /home/anshad/scripts/backup.sh`

### 5. SSL/TLS Configuration (Cloudflare)
Set in Cloudflare Dashboard:
- SSL/TLS mode: **Full (strict)**
- Always Use HTTPS: **On**
- Automatic HTTPS Rewrites: **On**

**Purpose:** Secure HTTPS connections
**Note:** Cloudflare handles certificates; no local SSL needed

---

## Phase 10: Final Configuration Summary

### Access URLs
| Service | URL | Credentials | Status |
|---------|-----|-------------|--------|
| Coolify | http://cool.plan5d.com | Setup on first access | ✅ HTTP 302 |
| Guacamole | http://guac.plan5d.com/guacamole/ | guacadmin / Nopassword.123 | ✅ HTTP 200 |

### Credentials
- **Guacamole Username:** `guacadmin`
- **Guacamole Password:** `Nopassword.123`
- **Connections:** Linux Desktop (RDP → localhost:3389), XRDP Session

### Data Locations
| Data Type | Path | Backup Priority |
|-----------|------|-----------------|
| Guacamole Config | ~/data/guacamole/user-mapping.xml | HIGH |
| Coolify Data | /data/coolify/ | HIGH |
| Projects | ~/projects/ | HIGH |
| Scripts | ~/scripts/ | MEDIUM |

### Maintenance Commands

```bash
# Check service status
~/scripts/health-check.sh

# Backup data
~/scripts/backup.sh /backup/destination

# Update containers
~/scripts/update.sh

# View Guacamole logs
sudo docker logs guacamole --tail 50

# Restart services
sudo docker restart guacamole
sudo systemctl restart docker

# Check Cloudflared
ps aux | grep cloudflared
tail -f /tmp/cloudflared.log
```

### Quick Verification
```bash
# Test Coolify
curl -I http://cool.plan5d.com

# Test Guacamole
curl -I http://guac.plan5d.com/guacamole/

# Test login
curl -s -X POST "http://guac.plan5d.com/guacamole/api/tokens" \
  -d "username=guacadmin&password=Nopassword.123" \
  -H "Content-Type: application/x-www-form-urlencoded"
```

---

## Next Steps for GitHub Projects

### 1. Initialize Python Project with uv
```bash
cd ~/projects
mkdir my-project
cd my-project
uv init

# Add dependencies
uv add pandas numpy matplotlib requests

# Create virtual environment
uv venv
source .venv/bin/activate

# Run your code
python main.py
```

### 2. Project Structure Template
```
~/projects/my-project/
├── .venv/                 # uv virtual environment
├── src/
│   └── main.py
├── tests/
├── pyproject.toml         # uv manages this
├── README.md
└── requirements.lock      # uv generates this
```

### 3. Development Workflow
```bash
# Activate environment
source ~/projects/my-project/.venv/bin/activate

# Install new packages
uv add <package-name>

# Run tests
python -m pytest

# Sync dependencies
uv sync
```

---

## Final Status

| Component | Status | Details |
|-----------|--------|---------|
| Coolify | ✅ Running | v4.0.0-beta.473 @ cool.plan5d.com |
| Guacamole | ✅ Running | v1.6.0 @ guac.plan5d.com, password: Nopassword.123 |
| Cloudflared | ✅ Running | Tunnel active, 2 domains configured |
| Docker | ✅ Running | v29.1.3, containers: coolify, guacamole, mysql |
| uv | ✅ Installed | v0.11.7 at ~/.local/bin/ |
| Directories | ✅ Created | projects, services, data, scripts |
| Documentation | ✅ Complete | Saved to GitHub repo |

### Backup Reminder
**Daily:** `0 2 * * * /home/anshad/scripts/backup.sh`
**Weekly Updates:** `0 3 * * 0 /home/anshad/scripts/update.sh`
**Health Checks:** Run manually or via cron as needed

---

**Setup Complete! All services running with custom domains and secure credentials.**